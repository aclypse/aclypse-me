import React, { FC } from "react";

import Layout from "@components/layout";

const ContactPage: FC<{}> = () => {
  return (
    <Layout>
      <main>
        <h2>Contact page</h2>
        <div>
          <p>Some contact (social networks info)</p>
        </div>
      </main>
    </Layout>
  );
};

export default ContactPage;
